# Staged changes introduced during the demo. Not part of the corpus, and must stay invisible to
# OpenWiki until deliberately copied into place.
demo/

# Editor and OS noise. An untracked or unignored stray file makes OpenWiki's no-op check bail to
# a full model run, so keep this list current.
.DS_Store
*.swp
.vscode/
.idea/

# Generated compile bookkeeping, written by the refresh workflow. Tracked in git
# so the agent and the UI can read it from the tree, but excluded from the source
# fingerprint: a tracked file that changes on every compile would make each
# compile look like a corpus change and trigger the next one forever.
.compile-state.json
.claims-index.json

# Repository machinery, not research knowledge. The workflow file in particular
# is edited throughout phase 04, and every edit would otherwise register as a
# corpus change and force a full 20-page regeneration.
.github/

# Instructions addressed to coding agents working ON this repo, as opposed to
# the research and regulatory documents the wiki is built FROM.
AGENTS.md
CLAUDE.md

# Phase 04 workflow machinery: the claims-delta, index-builder and callback
# scripts, the vendored index code, and the compile-state schema. Not research
# knowledge, and Python in the source fingerprint would make OpenWiki try to
# document it as code.
scripts/
contracts/
